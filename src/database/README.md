# Database Directory

This directory contains the SQLite database file for Black Sultan OS.

## Files
- `app.db` - SQLite database file (ignored by git)

## Note
The database file is automatically created when the application runs for the first time.
Do not commit database files to version control.
